
const makeDraw3d = (gl, linker, meshLoader, texLoader) => {
    // Rendering Pass
    const render = () => {
    };

    // Objects
    const makeCamera = () => {
        return {};
    };
    const makeLight = () => {
        return {};
    };
    const makeMesh = () => {
        return {};
    };

    return {
        render: render,
        makeCamera: makeCamera,
        makeLight: makeLight,
        makeMesh: makeMesh,
    };
};
