
const makeLvPlayer = (browser, graphics) => {
    const controller = browser.makeController();
    const pos = [0, 0, 0];
    const dir = [0, 0];

    const pre = () => {
    };
    const post = () => {
    };
    return {
        pre: pre,
        post: post
    }
};
